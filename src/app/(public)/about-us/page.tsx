// This file is a placeholder to prevent a parallel route conflict.
// It should not export a default React component and therefore should not be treated as a page.
// The "/about-us" route is intended to be served by /src/app/about-us/page.tsx (if active)
// or not at all if the "About Us" page has been removed.

export {}; // Exporting an empty object ensures this is not a page module.
